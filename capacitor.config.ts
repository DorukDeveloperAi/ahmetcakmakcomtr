import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'tr.com.ahmetcakmak.portfolio',
  appName: 'Ahmet Cakmak Portfolio',
  webDir: 'dist'
};

export default config;
